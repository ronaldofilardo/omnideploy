{
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "regions": ["gru1"],
  "buildCommand": "pnpm check-deps && pnpm prisma:generate && next build",
  "installCommand": "pnpm install",
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "true"
  }
}